# Windows 高内存占用

适用场景：

- 内存使用率持续较高
- 用户报告设备卡顿或程序响应缓慢

可能原因：

- 同时运行大量应用
- 浏览器标签页过多
- IDE、虚拟机等应用
- 后台程序持续占用
- 物理内存容量不足

诊断建议：

- 结合 `process_list` 判断主要内存占用来源。
- 结合 `device_get_info` 判断总内存容量。
- 单次高占用不能直接判断为硬件不足。
- 如果正常使用场景下长期接近满载，可以认为存在容量瓶颈。

处理建议：

- 优先关闭不必要应用。
- 减少后台程序。
- 长期容量瓶颈时可考虑增加物理内存。
