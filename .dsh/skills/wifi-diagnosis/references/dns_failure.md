# DNS 解析失败

适用场景：

- Wi-Fi 已连接；
- 默认网关可达；
- `wifi_diagnose` 返回 `dns_resolution = failed`。

判断原则：

- DNS 失败不代表无线网卡损坏；
- 如果网关也不可达，应先处理本地连接或路由器问题；
- VPN、代理、安全软件或组织网络策略可能改变 DNS 行为。

Demo 建议：

1. 说明当前检测到的故障层级；
2. 可帮助用户打开网络设置；
3. 第一版不自动修改 DNS 或重置网络配置。
