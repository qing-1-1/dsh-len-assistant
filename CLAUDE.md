# CLAUDE.md

**请阅读 [AGENTS.md](AGENTS.md) —— 那是本仓库 agent 说明的唯一事实来源。**

本文件刻意只放这一句指针，不放任何实际内容。

## 为什么是指针而不是副本

两份内容相同的文件必然会漂移：改了一份忘了另一份，然后两份说法冲突，
下一个人不知道该信哪份。同步脚本能缓解但不能根治——脚本会被忘记执行。

指针从结构上根治了这个问题：**只存在一份内容，没有第二份可以漂移。**
代价是 agent 要多读一个文件，这个代价远小于两份说明互相矛盾。

`npm test` 里有守卫会检查本文件没有长出内容。要加约定请写进 `AGENTS.md`。
